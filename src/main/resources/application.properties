#spring.queries.users-query=select email, password, active from user where email = ?
#spring.queries.roles-query=select u.email, r.role from user u inner join user_role ur on(u.user_id=ur.user_id) inner join role r on(ur.role_id=r.role_id) where u.email=?

spring.datasource.initialization-mode=always
spring.resources.chain.strategy.content.enabled=true
spring.resources.chain.strategy.content.paths=/**
spring.main.lazy-initialization=true
# max connection call to database
spring.datasource.hikari.maximum-pool-size=10000

spring.datasource.url=jdbc:mysql://localhost:3306/bonsai?useSSL=true
spring.datasource.username=root
spring.datasource.password=misasme

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=update
#Set name JPA Table with underscore
#spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot..naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=true

#logging.level.org.springframework.web: DEBUG
#logging.level.org.hibernate: ERROR

spring.thymeleaf.prefix=classpath:/templates
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/E/MyJava